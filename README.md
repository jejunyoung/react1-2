# 제준영 (202030329)
- ```
  6/5 in readme

  2024년 6월 5일 수업 내용
  ```	

## 2024년 6월 5일 수업 내용

### 합성
- Composition
- 여러개의 컴포넌트를 합쳐서 새로운 컴포넌트를 만드는 것
- 특정 컴포넌트가 하위 컨포넌트를 포함하고 있는 형태
- prop을 사용해서 자식 엘리먼트르 사용(꼭 prop를 사용할 필요는 없음)

#### Contaiment
- props.children를 통해 하위 컴포넌트를 하나로 모아서 제공
- SplitPane를 사용해서 화면의 왼쪽 오른쪽으로 분할함
  - SplitPane을 사용해서 left, right 두개의 props를 정의

#### Specialization
- 특별한 케이스
- 범용적인 개념을 구별히 되게 구체화하는 것을 특수화

<!-- ------------------------------------------------------------------------- -->

## 2024년 5월 29일 수업 내용
[Input Null Value](#input-null-value)  
[Shared State](#shared-state)

### Input Null Value
- null 값을 넣어준다

### Shared State
- 공유괸 State
- 여러 개의 하위 컴포넌트에서 공통적으로 사용하는 경우

<!-- ------------------------------------------------------------------------- -->

## 2024년 5월 22일 수업 내용
[리스트](#리스트)  
[키](#키)  
[여려 컴포넌트 로드](#여려-컴포넌트-로드)  
[품](#품)  
[제어컴포넌트](#제어컴포넌트)

### 리스트
- 목록
- 같은 아이템을 순서대로 모아 놓은 것
- 배열 사용

### 키
- 모두 각자 공유
- 각 객체나 아이템을 구분할 수 있는 고유한 값
- 고유값

### 여려 컴포넌트 로드
- 맵 함수를 사용

### 품
- 양식
- 사용자로부터 입력을 받기 위한 사용하는 것

### 제어컴포넌트
- state를 사용하여 관리
- 사용자가 임력한 값에 접근하고 제어할 수 있도록 해주는 컴포넌트

<!-- ------------------------------------------------------------------------- -->

## 2024년 5월 8일 수업 내용
[Arguments 전달](#arguments-전달)  
[조건부 렌더링](#조건부-렌더링)  
[엘리면서 변수](#엘리면서-변수)  
[인라인 조건](#인라인-조건)

### Arguments 전달
- 함수를 정의 할때는 파라미터 혹은 매개변수
- 
  ```
    function foo(x,y){ //파라미터(Parameter), 매개변수
      return x+y
    }
    let bas = foo(5,10) //아규먼트(Argument), 인수
  ```
- 이벤트 핸들러에 매개변수를 전달해야 하는 경우가 많음
- event라는 매개변수는 리액트의 이벤트 객체를 의미

### 조건부 렌더링
- 조건에 따라서 렌더링 되는 것이 다르게 된다

### 엘리면서 변수
- 렌더링해야 될 컴포넌트를 변수처럼 사용

### 인라인 조건
- if 문을 사용하지 않고 동일한 효과를 내기 위해
- && 논리 연산자 사용
  - true && expression = expression

<!-- ------------------------------------------------------------------------- -->

## 2024년 5월 1일 수업 내용
[훅의 규칙](#훅의-규칙)  
[커스텀 훅](#커스텀-훅)  
[이벤트 핸들러 방식](#이벤트-핸들러-방식)  
[DOM과 React 핸들러 방식의 차이점](#dom과-react-핸들러-방식의-차이점)

### 훅의 규칙
- 최상위 레벨에서만 호출
  - 반복문, 조건문 등 보다 위 레벨
- 렌더링 될 때마다 같은 순서로 호출
- 함수형 컴포넌트, 커스텀 훅에서만 호출

### 커스텀 훅
- 필요에 따라 훅을 만들어 사용할 수 있음

### 이벤트 핸들러 방식
- DOM에서 클릭 이벤트를 처리하는 방식
- React에서 클릭 이벤츠를 처리하는 방식

### DOM과 React 핸들러 방식의 차이점
- 이벤트 이름이 onclick에서 onClick으로 변경
- 전달하려는 함수는 문자열에서 함수 그래도 전달

<!-- ------------------------------------------------------------------------- -->

## 2024년 4월 17일 수업 내용
[훅이란?](#훅이란)  
[useState](#useState)  
[useEffect](#useeffect)  
[useMemon](#usememo)

### 훅이란?
- 클래스형 컴포넌트에서는 statr를 정의하고 업데이트할때 사용
- ~~예전에는 함수형 컴포넌트는 별도의 state를 정의 하거나 사용 할 수 없음~~
- state와 생명주기 기능에 갈고리를 걸어 원하는 시점에 정해진 함수를 실행되도록 하는 함수
- 훅의 이름은 모두 'use'로 시작

### useState 
- 가장 많이 사용하는 Hook(훅)
- 함수형 컴포넌트에서 state를 사용하기 위한 Hook(훅)
- 함수의 리턴값은 배열의 형태

### useEffect
- useState와 함께 가장 많이 사용하는 Hook(훅)
- 사이드 임펙트를 수행하기 위한 함수
- 사이드 임팩트는 랜더링 외에 부수적인 코드
- 생명주기 함수와 같은 기능을 하나로 통합한 기능 제공
- 배열 안에 있는 변수중에 하나라도 값이 변경되었을때 임펙트 함수 실행
- 처음 렌더링 된 이후, 재 렌더링 이후

### useMemo
- useMemon(), Memoized value를 리턴하는 Hook(훅)
- 이전 계산값을 가지고 있기 때문에 연산량이 많은 작업에 반복을 피할 수 있음
- 렌더링이 일어나는 동안에 실행
- 렌더링이 일어나는 동안 실행되서는 안될 작업을 넣으면 않됨
- 메모리제이션(memoization)
- 배열 안에 있는 변수중에 하나라도 값이 변경되었을때 임펙트 함수 실행

### useRef
- 래퍼런스를 사용하기 위한 Hook(훅)
- 특정 컴포넌트에 접근할 수 있는 객체를 의미
- 컴포넌트가 마운트 해제 전까지 계속 유지 

<!-- ------------------------------------------------------------------------- -->

## 2024년 4월 3일 수업 내용
[컴포넌트 종류](#컴포넌트-종류)  
[컴포넌트 합성](#컴포넌트-합성)  
[컴포넌트 추출](#컴포넌트-추출)

### 컴포넌트 종류
- ~~리액트 초기에는 클래스형 컴포넌트~~
- Hook 개념이 나오고 나서는 함수형 컴포넌트 사용

#### 클래스 컴포넌트 
- 생명주기에 관해서 공부해야함 
- 복잡
- React.Component class로 부터 받음

#### 함수형 컴포넌트
- props를 받아 props 중에 키값을 넣어서 반환

### 컴포넌트 합성
- 여러 개의 컴포넌트를 함쳐서 하나의 컴포넌트를 만드는 것

### 컴포넌트 추출
- 하나의 컴포넌트를 쪼개서 여러개의 컴포넌트로 나누는 것
- 큰 컴포넌트에서 일부를 추출해서 새로운 컴포넌트를 만드는 것

### state란?
- 컴포넌트의 상태, 컴포넌트의 데이터를 의미
- 변경 가능한 데이터

### state의 특징
- 자바스크립트 객체
- 함수형에는 useState() 함수 사용
- ~~class에서는 this.state 사용~~
- 직접 수정이 불가능

### ~~생명주기란?~~
- 컴포넌트의 생성시점에서 종료시점을 것
- Mounting(실행), Updating(사용중), Unmounting(종료)이 있음
- 현제는 훅 함수를 사용

<!-- ------------------------------------------------------------------------- -->

## 2024년 3월 27일 수업 내용
[JSX란?](#jsx란)  
[엘리먼트란?](#엘리먼트란)  
[엘리먼트(리액트)의 생김새](#엘리먼트리액트의-생김새)  
[엘리먼트의 특증](#엘리먼트의-특증)  
[컴포넌트란?](#컴포넌트란)  
[Props란?](#props란)   
[pure 함수와 Impure 함수](#pure-함수와-impure-함수)  

### JSX란?
- 자바 스크립트와 XML를 합친 것
- 컴포넌트를 사용하지 위해 
- 내부 XML/HTML 코드를 자바스크립드로 변환 
- 가독성을 높여 주는 역할   
- JSX안에서 자바 스크립트문서를 사용하고 싶으면 {}안에다가 사용

#### JSX 장점 
- 코드의 간결
- 가독성 향상
- 보안이 강함

### 엘리먼트란?
- 리액트 앱을 구성하는 요소
- 리액트에서 가장 작은 빌딩 블럭들
- 웹사이트의 경우는 DOM 엘리먼트이며, HTML 요소   

#### 리액트 엘리먼트와 DOM 엘리먼트의 차이
- 리액트 엘리먼트는 Virtual DOM 의 형식으로 취함
  - 빠르다
- DOM 엘리먼트는 페이지의 모든 정보를 갖고 있음
  - 느리다

### 엘리먼트(리액트)의 생김새
- 자바스크립트 객체의 형태로 존재
- 마음대로 변경할 수 없는 불변성
- type 대신에 리액트 컴포넌트

### 엘리먼트의 특증
- 내용을 마음대로 변경할 수 없는 불변성20203

### 컴포넌트란?
- 리액트는 컴포넌트 기반의 구조
- 컴포넌트 재사용이 가능, 전체 코드랑을 줄일 수 있어서 개발시간 및 유지보수 비용 감소
- 자바스크립트 함수와 유사
- 입력은 Props가 담당하고 출력은  리액트 엘리먼트의 형태
- 객체 지향의 개념과 비슷
- 이름은 항상 대문자로 시작

#### 컴포넌트 구조란
- 작은 컴포넌트가 모여 큰 컴포넌티, 큰 컴포넌트가 모여 전체 페이지

### Props란?
- 컴포넌트의 속성
- 어떤 속성,Props를 넣느냐에 따라 다른 엘리먼트 출력
- 다양한 정보를 담고 있는 자바스크립트 객체

#### Props의 특징
- 읽기 전용 
- 속성이 다른 엘리먼트를 생성하려면 새로운 Props 를 컴포넌드에 전달

### Pure 함수와 Impure 함수
- pure 함수는 인수로 받은 정보가 함수 내부에서 변하지 않는 함수
- Impure 함수는 인수로 받은 정보가 함수 내부에서 변하는 함수

<!-- ------------------------------------------------------------------------- -->

## 2024년 3월 20일 수업 내용
[리액트란?](#리액트란)

### 리액트란?
- 사용자 인터페이스를 만들기 위한 ~~자바스크립트~~ 라이브러리  
- SPA 를 쉽고 빠르게 만들 수 있도록 해주는 도구
- 복잡한 사이드를 쉽고 빠르게 만들고, 관리하기 위한 도구

#### 리액트의 장점
- Virtual DOM (비동기식) 방법으로 사용해서 속도가 빠르다 (필요한 부분만 수정)
- 컴포넌트를 조합해서 개발하여 재사용성이 뛰어나다
- 메타에서 오픈소스 프로젝트로 관리하고 있어 계속 발전
- 크로스 플랫폼 모바일 앱 개발 가능
- 활발한 지식 공유(커뮤니티 활성화)

#### 리액트의 단점
- ~~방대한 학습량~~(자바스크립도를 공부한 경우 빠르게 학습가능)
- ~~높은 상태 관리 복잡도~~(18버전 이후 라이브 사이클를 잘 사용 안함)
- 너무 빠른 발전

<!-- ------------------------------------------------------------------------- -->

## 204년 3월 13일 수업 내용

[HTML란?](#html란)  
[CSS란?](#css란)  
[자바스크립트](#자바스크립트란)

### HTML란?
- 웹 사이트의 구조(뻐대)
- 대부분 페어로 사용
- < 문법/ > = HTML문법(X)
- SPA(Single Page Application) Page는 하나지만 필요한 요소만 교체

### CSS란?
- 웹 사이트의 스타일 시트(살)
- 반응형에 적합
- 반응형 웹에 작업량을 줄이기 위해 Sass사용

### 자바스크립트란?
- 웹 사이트의 기능(머리)
- 정적인 사이트에서 동적인 사이트로 변화하기 위해 사용

#### 자바스크립트 자료형
- var - 중복 선언 가능, 재할당 가능
- let -  중복 선언 불가능, 재할당 가능
- const - 중복 선언 불가능, 재할당 불가능 (상수)
- Array type - 배열(배열에 모든 다 가능)
- Object type - 속성
- json - 안에 모든 가능 (json 안에 json 가능)
- 대입 연산자 - 대입(=)
- 산술 연산자 - 산술(*,-,+,/)
- 증감 연산자 - 증가 혹은 감소
- 비교 연산자 - 비교 (2개는 모양만, 3개는 정확한)
- 삼항 연산자 - 조건부 연산자 내용 참 거짓 3개를 사용(a?b:c)
- function - 일반적은 함수(function foo(x,y)) {return x+y}
- 화살표 함수 - let woo = (x,y) => {return x+y} 객체를 만들어서 사용

<!-- ------------------------------------------------------------------------- -->

## 2024년 3월 6일 수업 내용
[버전 관리](#버전-관리)

### 버전 관리
[유의적 버전](#유의적-버전)  
[사용법](#사용법-ex-3145)

#### 유의적 버전 
- 개발자가 사용하는 버전
- 호환을 위해 사용하는 버전 생성 방법

#### 사용법 (Ex: 3.14.5)
- 첫번째 자리는 배포한 번호(1.0.0)
- 두번째 자리는 기능을 추가한 번호(0.1.0)
- 마지막 자리는 오류를 수정한 번호(0.0.1)
- 앞 번호가 올라가면 뒤에 번호는 초기화
